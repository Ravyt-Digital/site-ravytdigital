import type { Metadata } from "next";
import { notFound } from "next/navigation";
import Link from "next/link";
import { SITE_URL } from "@/lib/site";
import { getPost, posts } from "../posts";
export function generateStaticParams(){return posts.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const post=getPost(slug);if(!post)return{};return{title:post.title,description:post.excerpt,alternates:{canonical:`/blog/${slug}`},openGraph:{title:post.title,description:post.excerpt,type:"article",url:`/blog/${slug}`,images:[{url:"/brand/ravyt-social-card.jpg",width:1200,height:630,alt:"Ravyt Digital — Social Media para Psicólogos Parentais"}]}}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=getPost(slug);if(!post)notFound();const related=posts.filter(item=>item.slug!==post.slug).slice(0,2);const schema={"@context":"https://schema.org","@type":"BlogPosting",headline:post.title,description:post.excerpt,datePublished:post.date,dateModified:post.date,inLanguage:"pt-BR",mainEntityOfPage:{"@type":"WebPage","@id":`${SITE_URL}/blog/${post.slug}`},author:{"@id":`${SITE_URL}/autores/ytala-cabral#person`},publisher:{"@id":`${SITE_URL}/#organization`}};return <main id="conteudo" className="article-page"><article><header className="article-hero"><div className="shell article-hero-grid"><div><Link className="back-blog" href="/blog">← Voltar ao blog</Link><p className="article-category">{post.category}</p><h1>{post.title}</h1><p className="article-intro">{post.excerpt}</p><div className="article-meta"><span>Por <Link href="/autores/ytala-cabral">Ytala Cabral</Link></span><time dateTime={post.date}>{post.dateLabel}</time><span>{post.readingTime}</span></div></div></div></header><div className="article-layout shell"><div className="article-body"><nav className="article-toc" aria-label="Neste conteúdo"><strong>Neste conteúdo</strong>{post.sections.map(section=><a key={section.title} href={`#${section.title.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")}`}>{section.title}</a>)}</nav><p className="article-lead">{post.intro}</p>{post.sections.map((section)=><section id={section.title.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")} key={section.title}><h2>{section.title}</h2>{section.paragraphs.map((paragraph)=><p key={paragraph}>{paragraph}</p>)}</section>)}<aside className="editorial-note"><strong>Como este conteúdo foi produzido</strong><p>Este artigo reúne aprendizados da rotina de estratégia e produção de conteúdo da Ravyt para Psicologia Parental. O objetivo é orientar decisões de comunicação, sem substituir avaliação ou orientação psicológica.</p></aside><section className="related-posts"><h2>Continue aprofundando</h2>{related.map(item=><Link key={item.slug} href={`/blog/${item.slug}`}>{item.title} →</Link>)}</section><aside className="article-conclusion"><p>Social Media para Psicólogos Parentais</p><h2>Quer construir uma presença digital coerente com a profundidade do seu trabalho?</h2><Link className="button" href="/contato">Conversar com Ytala</Link></aside></div></div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></article></main>}
